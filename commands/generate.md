---
description: Generate persona cards and journey maps from insights (use --update to refresh existing ones)
---

Use the generate skill (${CLAUDE_PLUGIN_ROOT}/skills/generate/SKILL.md) to create persona cards and journey maps from personas/research/insights.md.

If arguments include `--update` (or cards already exist), run in update mode: $ARGUMENTS
