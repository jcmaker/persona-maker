## What & why

Briefly describe the change and the motivation.

## How I verified

- [ ] `python -m pytest tests/ -v` passes
- [ ] `node scripts/smoke_template.mjs` passes (if `template.html` changed)
- [ ] Opened a build in the browser with zero console errors (if `template.html` changed)
- [ ] Repo content stays in English; methodology changed in one place (`core/methodology/`)

## Notes for reviewers

Anything worth calling out — trade-offs, follow-ups, screenshots.
